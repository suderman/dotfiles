#!/bin/sh

# 2014 Jon Suderman
# https://github.com/suderman/local

# Create tar archive of config directory
sudo tar -pczf /data/backup/config.tar.gz /config

# Update permissions
sudo chown -R data:data /data
sudo chmod -R 775 /data
